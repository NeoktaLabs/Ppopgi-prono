import { useEffect, useMemo, useRef, useState } from "react";
import { api } from "./lib/api";

type User = { id: string; email: string; nickname: string | null; is_global_admin?: boolean };
type League = { id: string; name: string; code: string; role: string };
type Match = { id: string; home_team: string; away_team: string; kickoff_at: string; stage: string | null; points_multiplier: number; status: string; final_home: number | null; final_away: number | null; manual_final_home?: number | null; manual_final_away?: number | null; score_source?: "api" | "manual" | "none"; live_home_score?: number | null; live_away_score?: number | null; live_minute?: number | null; effective_home_score?: number | null; effective_away_score?: number | null; effective_final_home?: number | null; effective_final_away?: number | null; is_live?: boolean; predictions?: MatchPrediction[] };
type MatchPrediction = { user_id: string; nickname: string | null; home_score: number; away_score: number; points: number; live_points?: number };
type MyPrediction = { match_id: string; home_score: number; away_score: number; updated_at: string };
type PredictionDraft = { home: string; away: string };
type MeResponse = { user: User | null; leagues: League[] };
type LeaderboardRow = { user_id: string; nickname: string | null; points: number; rank: number; official_rank: number; rank_delta: number; movement_type: "live" | "last_match" };
type LeagueHome = { mode: "live" | "official"; leaderboard: LeaderboardRow[]; matches: Match[]; poll_seconds: number };
type Language = "en" | "fr";
type LeaguePage = "home" | "picks" | "worldcup" | "rules" | "profile" | "admin";
type Copy = Record<string, string>;

const copy: Record<Language, Copy> = {
  en: {
    loading: "Loading…", worldCup: "World Cup", intro: "Create or join a private league, predict matches, and climb the leaderboard.", emailPlaceholder: "you@example.com", sendLoginLink: "Send me a login link", magicLinkSent: "Magic link sent. Check your inbox.", welcome: "Welcome 👋", chooseNickname: "Choose the nickname that will appear in league standings.", nicknamePlaceholder: "Nickname", continue: "Continue", signedIn: "Signed in:", hi: "Hi", signOut: "Sign out", myLeagues: "My leagues", hubIntro: "Create a league, join one with a code, or open an existing league.", noLeagues: "No leagues yet.", openLeague: "Open league", backToLeagues: "← My leagues", code: "Code:", role: "Role:", leagueNamePlaceholder: "League name", create: "Create", leagueCodePlaceholder: "League code", join: "Join", liveLeaderboard: "Live leaderboard", officialLeaderboard: "Leaderboard", liveExplanation: "Provisional movement if live scores stay like this.", lastMatchExplanation: "Movement since the latest finalized match.", todaysMatches: "Today’s matches", allMatches: "All matches", noMatchesToday: "No matches today.", noMatchesLoaded: "No matches loaded yet. If you are the global admin, trigger a sync or wait for the cron job.", syncMatches: "Sync matches", syncStarted: "Match sync triggered. Refresh in a few seconds.", live: "LIVE", minute: "min", predictions: "Predictions", points: "pts", picksTitle: "Make predictions", picksIntro: "Enter your scores. They autosave once both scores are filled and lock at kickoff.", worldCupTitle: "World Cup matches", rulesTitle: "Rules", rulesText: "Predictions lock at kickoff. Other players’ picks are hidden until the match starts. Knockout multipliers are x2, x3, x4 and x5 for the final.", profileTitle: "Profile", saveProfile: "Save profile", profileSaved: "Profile updated.", standings: "🏆 Home", picks: "⚽ Picks", worldCupNav: "📅 World Cup", rules: "📜 Rules", profile: "👤 Profile", yourPick: "Your pick", saving: "Saving…", saved: "Saved", locked: "Locked", homeScore: "Home", awayScore: "Away", admin: "🛠 Admin", adminTitle: "Global admin", adminIntro: "Override final scores globally for every league, recalculate points, or clear a manual override.", setFinalScore: "Set final score", clearOverride: "Clear override", recalculate: "Recalculate", manual: "Manual", apiScore: "API"
  },
  fr: {
    loading: "Chargement…", worldCup: "Coupe du Monde", intro: "Crée ou rejoins une ligue privée, pronostique les matches et grimpe au classement.", emailPlaceholder: "toi@example.com", sendLoginLink: "Recevoir mon lien de connexion", magicLinkSent: "Lien magique envoyé. Vérifie ta boîte e-mail.", welcome: "Bienvenue 👋", chooseNickname: "Choisis le pseudo qui apparaîtra dans les classements.", nicknamePlaceholder: "Pseudo", continue: "Continuer", signedIn: "Connecté :", hi: "Salut", signOut: "Déconnexion", myLeagues: "Mes ligues", hubIntro: "Crée une ligue, rejoins-en une avec un code ou ouvre une ligue existante.", noLeagues: "Aucune ligue pour le moment.", openLeague: "Ouvrir la ligue", backToLeagues: "← Mes ligues", code: "Code :", role: "Rôle :", leagueNamePlaceholder: "Nom de la ligue", create: "Créer", leagueCodePlaceholder: "Code de ligue", join: "Rejoindre", liveLeaderboard: "Classement live", officialLeaderboard: "Classement", liveExplanation: "Mouvement provisoire si les scores live restent comme ça.", lastMatchExplanation: "Mouvement depuis le dernier match finalisé.", todaysMatches: "Matches du jour", allMatches: "Tous les matches", noMatchesToday: "Aucun match aujourd’hui.", noMatchesLoaded: "Aucun match chargé pour le moment. Si tu es admin global, lance une synchro ou attends le cron.", syncMatches: "Synchroniser les matches", syncStarted: "Synchro lancée. Rafraîchis dans quelques secondes.", live: "LIVE", minute: "min", predictions: "Pronostics", points: "pts", picksTitle: "Faire mes pronostics", picksIntro: "Entre tes scores. Ils sont sauvegardés automatiquement quand les deux scores sont remplis et verrouillés au coup d’envoi.", worldCupTitle: "Matches de la Coupe du Monde", rulesTitle: "Règles", rulesText: "Les pronostics sont verrouillés au coup d’envoi. Les pronos des autres sont cachés jusqu’au début du match. Les multiplicateurs sont x2, x3, x4 et x5 pour la finale.", profileTitle: "Profil", saveProfile: "Sauvegarder le profil", profileSaved: "Profil mis à jour.", standings: "🏆 Accueil", picks: "⚽ Pronos", worldCupNav: "📅 Coupe du Monde", rules: "📜 Règles", profile: "👤 Profil", yourPick: "Ton prono", saving: "Sauvegarde…", saved: "Sauvegardé", locked: "Verrouillé", homeScore: "Domicile", awayScore: "Extérieur", admin: "🛠 Admin", adminTitle: "Admin global", adminIntro: "Modifie les scores finaux globalement pour toutes les ligues, recalcule les points ou supprime un override manuel.", setFinalScore: "Définir le score final", clearOverride: "Supprimer override", recalculate: "Recalculer", manual: "Manuel", apiScore: "API"
  },
};

function getInitialLanguage(): Language { const stored = localStorage.getItem("language"); if (stored === "en" || stored === "fr") return stored; return navigator.language.toLowerCase().startsWith("fr") ? "fr" : "en"; }
function LanguageToggle({ language, setLanguage }: { language: Language; setLanguage: (language: Language) => void }) { return <div className="language-toggle" aria-label="Language selector"><button className={language === "en" ? "active" : ""} onClick={() => setLanguage("en")}>EN</button><button className={language === "fr" ? "active" : ""} onClick={() => setLanguage("fr")}>FR</button></div>; }
function Movement({ delta }: { delta: number }) { if (delta > 0) return <span className="movement up">▲{delta}</span>; if (delta < 0) return <span className="movement down">▼{Math.abs(delta)}</span>; return <span className="movement neutral">—</span>; }

function MatchCard({ match, language, t }: { match: Match; language: Language; t: Copy }) {
  return <article className={`match ${match.is_live ? "live-match" : ""}`}><div className="match-head">{match.is_live && <span className="live-pill"><span className="pulse" />{t.live} {match.live_minute ? `${match.live_minute} ${t.minute}` : ""}</span>}<span className="badge">x{match.points_multiplier}</span></div><h3>{match.home_team} {match.effective_home_score ?? match.final_home ?? "-"} - {match.effective_away_score ?? match.final_away ?? "-"} {match.away_team}</h3><p>{new Date(match.kickoff_at).toLocaleString(language === "fr" ? "fr-CH" : "en-GB")}</p><p>{match.stage ?? match.status} · {match.status}</p>{match.score_source === "manual" && <p className="notice">{t.manual}: {match.manual_final_home}-{match.manual_final_away}</p>}{match.predictions && match.predictions.length > 0 && <div className="prediction-list"><h4>{t.predictions}</h4>{match.predictions.map((prediction) => <div className="prediction-row" key={prediction.user_id}><span>{prediction.nickname ?? "—"}</span><span>{prediction.home_score}-{prediction.away_score}</span><strong>{prediction.live_points ?? prediction.points} {t.points}</strong></div>)}</div>}</article>;
}

function PickCard({ match, language, t, draft, status, onChange }: { match: Match; language: Language; t: Copy; draft: PredictionDraft; status?: string; onChange: (field: "home" | "away", value: string) => void }) {
  const locked = Date.now() >= new Date(match.kickoff_at).getTime();
  return <article className="match pick-card"><div className="match-head"><span className="badge">x{match.points_multiplier}</span><span className={locked ? "save-status locked" : "save-status"}>{locked ? t.locked : status}</span></div><h3>{match.home_team} - {match.away_team}</h3><p>{new Date(match.kickoff_at).toLocaleString(language === "fr" ? "fr-CH" : "en-GB")}</p><p>{match.stage ?? match.status}</p><div className="pick-inputs"><label><span>{match.home_team}</span><input disabled={locked} inputMode="numeric" min="0" type="number" value={draft.home} onChange={(e) => onChange("home", e.target.value)} placeholder="-" /></label><strong>-</strong><label><span>{match.away_team}</span><input disabled={locked} inputMode="numeric" min="0" type="number" value={draft.away} onChange={(e) => onChange("away", e.target.value)} placeholder="-" /></label></div></article>;
}

function AdminScoreCard({ match, language, t, draft, status, onChange, onSet, onClear, onRecalculate }: { match: Match; language: Language; t: Copy; draft: PredictionDraft; status?: string; onChange: (field: "home" | "away", value: string) => void; onSet: () => void; onClear: () => void; onRecalculate: () => void }) {
  return <article className="match pick-card"><div className="match-head"><span className="badge">x{match.points_multiplier}</span><span className="save-status">{status}</span></div><h3>{match.home_team} - {match.away_team}</h3><p>{new Date(match.kickoff_at).toLocaleString(language === "fr" ? "fr-CH" : "en-GB")}</p><p>{match.stage ?? match.status} · {match.score_source === "manual" ? `${t.manual}: ${match.manual_final_home}-${match.manual_final_away}` : `${t.apiScore}: ${match.final_home ?? "-"}-${match.final_away ?? "-"}`}</p><div className="pick-inputs"><label><span>{match.home_team}</span><input inputMode="numeric" min="0" type="number" value={draft.home} onChange={(e) => onChange("home", e.target.value)} placeholder="-" /></label><strong>-</strong><label><span>{match.away_team}</span><input inputMode="numeric" min="0" type="number" value={draft.away} onChange={(e) => onChange("away", e.target.value)} placeholder="-" /></label></div><div className="admin-actions"><button onClick={onSet}>{t.setFinalScore}</button><button className="ghost" onClick={onClear}>{t.clearOverride}</button><button className="ghost" onClick={onRecalculate}>{t.recalculate}</button></div></article>;
}

function App() {
  const [language, setLanguageState] = useState<Language>(getInitialLanguage);
  const t = copy[language];
  const [me, setMe] = useState<MeResponse | null>(null);
  const [home, setHome] = useState<LeagueHome | null>(null);
  const [email, setEmail] = useState("");
  const [nickname, setNickname] = useState("");
  const [leagueName, setLeagueName] = useState("");
  const [joinCode, setJoinCode] = useState("");
  const [matches, setMatches] = useState<Match[]>([]);
  const [allMatches, setAllMatches] = useState<Match[]>([]);
  const [drafts, setDrafts] = useState<Record<string, PredictionDraft>>({});
  const [adminDrafts, setAdminDrafts] = useState<Record<string, PredictionDraft>>({});
  const [saveStatus, setSaveStatus] = useState<Record<string, string>>({});
  const [adminStatus, setAdminStatus] = useState<Record<string, string>>({});
  const saveTimers = useRef<Record<string, ReturnType<typeof setTimeout>>>({});
  const [message, setMessage] = useState("");
  const [selectedLeagueId, setSelectedLeagueId] = useState<string | null>(() => localStorage.getItem("selectedLeagueId"));
  const [page, setPage] = useState<LeaguePage>("home");
  const currentLeague = useMemo(() => me?.leagues.find((league) => league.id === selectedLeagueId) ?? null, [me, selectedLeagueId]);

  function setLanguage(nextLanguage: Language) { setLanguageState(nextLanguage); localStorage.setItem("language", nextLanguage); }
  function openLeague(leagueId: string) { setSelectedLeagueId(leagueId); localStorage.setItem("selectedLeagueId", leagueId); setPage("home"); loadLeagueHome(leagueId).catch(() => undefined); }
  function closeLeague() { setSelectedLeagueId(null); localStorage.removeItem("selectedLeagueId"); setHome(null); setPage("home"); }
  async function loadLeagueHome(leagueId: string) { const nextHome = await api<LeagueHome>(`/api/leagues/${leagueId}/home`); setHome(nextHome); setMatches(nextHome.matches); return nextHome; }
  async function loadAllMatches() { const data = await api<{ matches: Match[] }>("/api/worldcup/matches"); setAllMatches(data.matches); const nextAdminDrafts: Record<string, PredictionDraft> = {}; for (const match of data.matches) nextAdminDrafts[match.id] = { home: String(match.manual_final_home ?? match.final_home ?? ""), away: String(match.manual_final_away ?? match.final_away ?? "") }; setAdminDrafts(nextAdminDrafts); return data.matches; }
  async function loadMyPredictions(leagueId: string) { const data = await api<{ predictions: MyPrediction[] }>(`/api/leagues/${leagueId}/predictions/me`); const next: Record<string, PredictionDraft> = {}; for (const prediction of data.predictions) next[prediction.match_id] = { home: String(prediction.home_score), away: String(prediction.away_score) }; setDrafts(next); }
  async function refresh() { const nextMe = await api<MeResponse>("/api/auth/me"); setMe(nextMe); const selected = nextMe.leagues.find((league) => league.id === selectedLeagueId); if (selected) await loadLeagueHome(selected.id); }

  function updatePrediction(match: Match, field: "home" | "away", value: string) {
    if (!currentLeague || Date.now() >= new Date(match.kickoff_at).getTime()) return;
    const clean = value.replace(/[^0-9]/g, "").slice(0, 2);
    const current = drafts[match.id] ?? { home: "", away: "" };
    const next = { ...current, [field]: clean };
    setDrafts((previous) => ({ ...previous, [match.id]: next }));
    if (saveTimers.current[match.id]) clearTimeout(saveTimers.current[match.id]);
    if (next.home === "" || next.away === "") { setSaveStatus((previous) => ({ ...previous, [match.id]: "" })); return; }
    setSaveStatus((previous) => ({ ...previous, [match.id]: t.saving }));
    saveTimers.current[match.id] = setTimeout(async () => {
      try {
        await api(`/api/leagues/${currentLeague.id}/predictions`, { method: "POST", body: JSON.stringify({ matchId: match.id, homeScore: Number(next.home), awayScore: Number(next.away) }) });
        setSaveStatus((previous) => ({ ...previous, [match.id]: t.saved }));
      } catch (error) {
        setSaveStatus((previous) => ({ ...previous, [match.id]: error instanceof Error ? error.message : "Error" }));
      }
    }, 500);
  }

  function updateAdminDraft(match: Match, field: "home" | "away", value: string) { const clean = value.replace(/[^0-9]/g, "").slice(0, 2); const current = adminDrafts[match.id] ?? { home: "", away: "" }; setAdminDrafts((previous) => ({ ...previous, [match.id]: { ...current, [field]: clean } })); }
  async function setManualScore(match: Match) { const draft = adminDrafts[match.id]; if (!draft || draft.home === "" || draft.away === "") return; setAdminStatus((previous) => ({ ...previous, [match.id]: t.saving })); await api(`/api/admin/matches/${match.id}/manual-score`, { method: "POST", body: JSON.stringify({ homeScore: Number(draft.home), awayScore: Number(draft.away) }) }); setAdminStatus((previous) => ({ ...previous, [match.id]: t.saved })); await loadAllMatches(); if (currentLeague) await loadLeagueHome(currentLeague.id); }
  async function clearManualScore(match: Match) { setAdminStatus((previous) => ({ ...previous, [match.id]: t.saving })); await api(`/api/admin/matches/${match.id}/manual-score`, { method: "DELETE" }); setAdminStatus((previous) => ({ ...previous, [match.id]: t.saved })); await loadAllMatches(); if (currentLeague) await loadLeagueHome(currentLeague.id); }
  async function recalculateAdminMatch(match: Match) { setAdminStatus((previous) => ({ ...previous, [match.id]: t.saving })); await api(`/api/admin/matches/${match.id}/recalculate`, { method: "POST" }); setAdminStatus((previous) => ({ ...previous, [match.id]: t.saved })); if (currentLeague) await loadLeagueHome(currentLeague.id); }

  useEffect(() => { refresh().catch(() => setMe({ user: null, leagues: [] })); }, []);
  useEffect(() => { if (page === "worldcup" || page === "picks" || page === "admin") loadAllMatches().catch(() => undefined); }, [page]);
  useEffect(() => { if (page === "picks" && currentLeague) loadMyPredictions(currentLeague.id).catch(() => undefined); }, [page, currentLeague?.id]);
  useEffect(() => { if (!currentLeague) return; const seconds = home?.poll_seconds ?? 60; const id = window.setInterval(() => { loadLeagueHome(currentLeague.id).catch(() => undefined); }, seconds * 1000); return () => window.clearInterval(id); }, [currentLeague?.id, home?.poll_seconds]);

  async function requestLink() { await api("/api/auth/request-link", { method: "POST", body: JSON.stringify({ email }) }); setMessage(t.magicLinkSent); }
  async function saveNickname() { await api("/api/me/profile", { method: "PATCH", body: JSON.stringify({ nickname }) }); setMessage(t.profileSaved); await refresh(); }
  async function createLeague() { const league = await api<{ id: string }>("/api/leagues", { method: "POST", body: JSON.stringify({ name: leagueName }) }); setLeagueName(""); await refresh(); openLeague(league.id); }
  async function joinLeague() { const joined = await api<{ leagueId: string }>("/api/leagues/join", { method: "POST", body: JSON.stringify({ code: joinCode }) }); setJoinCode(""); await refresh(); openLeague(joined.leagueId); }
  async function logout() { await api("/api/auth/logout", { method: "POST" }); setMe({ user: null, leagues: [] }); setHome(null); closeLeague(); }
  async function syncMatches() { await api("/api/admin/sync/matches", { method: "POST" }); setMessage(t.syncStarted); await loadAllMatches(); if (currentLeague) await loadLeagueHome(currentLeague.id); }

  if (!me) return <main className="shell"><div className="card"><LanguageToggle language={language} setLanguage={setLanguage} />{t.loading}</div></main>;
  if (!me.user) return <main className="shell hero"><section className="card hero-card"><LanguageToggle language={language} setLanguage={setLanguage} /><p className="eyebrow">{t.worldCup}</p><h1>Ppopgi Prono ⚽</h1><p className="lead">{t.intro}</p><div className="form-row"><input value={email} onChange={(e) => setEmail(e.target.value)} placeholder={t.emailPlaceholder} type="email" /><button onClick={requestLink}>{t.sendLoginLink}</button></div>{message && <p className="notice">{message}</p>}</section></main>;
  if (!me.user.nickname) return <main className="shell"><section className="card"><LanguageToggle language={language} setLanguage={setLanguage} /><h1>{t.welcome}</h1><p>{t.chooseNickname}</p><div className="form-row"><input value={nickname} onChange={(e) => setNickname(e.target.value)} placeholder={t.nicknamePlaceholder} /><button onClick={saveNickname}>{t.continue}</button></div></section></main>;

  return <main className="shell"><header className="topbar"><div><p className="eyebrow">{t.signedIn} {me.user.email}</p><h1>{currentLeague ? currentLeague.name : `${t.hi} ${me.user.nickname} 👋`}</h1></div><div className="top-actions"><LanguageToggle language={language} setLanguage={setLanguage} /><button className="ghost" onClick={logout}>{t.signOut}</button></div></header>{message && <p className="notice">{message}</p>}{!currentLeague ? <section className="grid"><div className="card"><h2>{t.myLeagues}</h2><p className="muted">{t.hubIntro}</p>{me.leagues.length === 0 ? <p>{t.noLeagues}</p> : <ul className="league-list">{me.leagues.map((league) => <li key={league.id}><div><strong>{league.name}</strong><span>{t.code} {league.code}</span><span>{t.role} {league.role}</span></div><button onClick={() => openLeague(league.id)}>{t.openLeague}</button></li>)}</ul>}</div><div className="card"><h2>{t.create}</h2><div className="split"><input value={leagueName} onChange={(e) => setLeagueName(e.target.value)} placeholder={t.leagueNamePlaceholder} /><button onClick={createLeague}>{t.create}</button></div><h2>{t.join}</h2><div className="split"><input value={joinCode} onChange={(e) => setJoinCode(e.target.value.toUpperCase())} placeholder={t.leagueCodePlaceholder} /><button onClick={joinLeague}>{t.join}</button></div></div></section> : <><button className="ghost back-link" onClick={closeLeague}>{t.backToLeagues}</button>{page === "home" && <section className="grid"><div className="card"><h2>{home?.mode === "live" ? t.liveLeaderboard : t.officialLeaderboard}</h2><p className="muted">{home?.mode === "live" ? t.liveExplanation : t.lastMatchExplanation}</p><div className="leaderboard-live">{(home?.leaderboard ?? []).slice(0, 12).map((row) => <div className="leaderboard-row" key={row.user_id}><span className="rank">#{row.rank}</span><strong>{row.nickname ?? "—"}</strong><span>{row.points} {t.points}</span><Movement delta={row.rank_delta} /></div>)}</div></div><div className="card"><h2>{t.todaysMatches}</h2>{matches.length === 0 ? <p className="muted">{t.noMatchesToday}</p> : <div className="matches">{matches.map((match) => <MatchCard key={match.id} match={match} language={language} t={t} />)}</div>}</div></section>}{page === "picks" && <section className="card"><div className="section-head"><div><h2>{t.picksTitle}</h2><p className="muted">{t.picksIntro}</p></div><button className="ghost" onClick={syncMatches}>{t.syncMatches}</button></div>{allMatches.length === 0 ? <p className="muted">{t.noMatchesLoaded}</p> : <div className="matches">{allMatches.map((match) => <PickCard key={match.id} match={match} language={language} t={t} draft={drafts[match.id] ?? { home: "", away: "" }} status={saveStatus[match.id]} onChange={(field, value) => updatePrediction(match, field, value)} />)}</div>}</section>}{page === "worldcup" && <section className="card"><div className="section-head"><div><h2>{t.worldCupTitle}</h2><p className="muted">{t.allMatches}</p></div><button className="ghost" onClick={syncMatches}>{t.syncMatches}</button></div>{allMatches.length === 0 ? <p className="muted">{t.noMatchesLoaded}</p> : <div className="matches">{allMatches.map((match) => <MatchCard key={match.id} match={match} language={language} t={t} />)}</div>}</section>}{page === "admin" && me.user.is_global_admin && <section className="card"><div className="section-head"><div><h2>{t.adminTitle}</h2><p className="muted">{t.adminIntro}</p></div><button className="ghost" onClick={syncMatches}>{t.syncMatches}</button></div>{allMatches.length === 0 ? <p className="muted">{t.noMatchesLoaded}</p> : <div className="matches">{allMatches.map((match) => <AdminScoreCard key={match.id} match={match} language={language} t={t} draft={adminDrafts[match.id] ?? { home: "", away: "" }} status={adminStatus[match.id]} onChange={(field, value) => updateAdminDraft(match, field, value)} onSet={() => setManualScore(match)} onClear={() => clearManualScore(match)} onRecalculate={() => recalculateAdminMatch(match)} />)}</div>}</section>}{page === "rules" && <section className="card"><h2>{t.rulesTitle}</h2><p className="lead">{t.rulesText}</p></section>}{page === "profile" && <section className="card"><h2>{t.profileTitle}</h2><p className="muted">{me.user.email}</p><div className="split"><input value={nickname} onChange={(e) => setNickname(e.target.value)} placeholder={me.user.nickname ?? t.nicknamePlaceholder} /><button onClick={saveNickname}>{t.saveProfile}</button></div></section>}<nav className={`bottom-nav ${me.user.is_global_admin ? "with-admin" : ""}`}><button className={page === "home" ? "active" : ""} onClick={() => setPage("home")}>{t.standings}</button><button className={page === "picks" ? "active" : ""} onClick={() => setPage("picks")}>{t.picks}</button><button className={page === "worldcup" ? "active" : ""} onClick={() => setPage("worldcup")}>{t.worldCupNav}</button><button className={page === "rules" ? "active" : ""} onClick={() => setPage("rules")}>{t.rules}</button><button className={page === "profile" ? "active" : ""} onClick={() => setPage("profile")}>{t.profile}</button>{me.user.is_global_admin && <button className={page === "admin" ? "active" : ""} onClick={() => setPage("admin")}>{t.admin}</button>}</nav></>}</main>;
}
export default App;
